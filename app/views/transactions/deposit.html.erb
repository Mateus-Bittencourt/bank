<div class="container">
  <h2>Deposito</h2>
   <%= simple_form_for([@user, @transaction], method: :post, url: save_deposit_path(current_user), data: { turbo: :false }) do |f| %>
    <%= f.input :amount %>
    <%= f.input :password %>
    <%= f.button :submit, 'Depositar' %>
  <% end %>
</div>
